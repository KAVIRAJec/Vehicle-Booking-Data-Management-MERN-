import React from 'react';
import styled from 'styled-components';

// Define the styled component
const RV = styled.div.withConfig({ displayName: "ArrowButton__ButtonWrapper", componentId: "sc-1ikb0hj-0" })(["@media screen and (max-width:", "px){display:none;}"]);

// Define the ArrowButton component
const ArrowButton = () => {
  return (
    <RV>
      
    </RV>
  );
};

export default ArrowButton;